/**
 * Intent_Session Manager module. Opens, closes, and modifies Intent_Sessions, associates
 * Command_Events with the active session, and enforces the invariant that an Agent_Actor can
 * never open, expand, or modify intent.
 */
package com.intentguard.intent;
