/**
 * Scenario module. Seeds each scripted demo scenario from a frozen baseline Behavioral_Profile
 * and Threshold_Configuration and replays the scripted Command_Events deterministically (with a
 * stubbed LLM) so replays are reproducible for the demo.
 */
package com.intentguard.scenario;
