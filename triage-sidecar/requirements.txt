# Runtime dependencies (pinned) for the Alert Triage Sidecar.
# Install with:  pip install -r requirements.txt
langgraph==0.2.76
pydantic==2.9.2

# Test / development dependencies (pinned).
# Install with:  pip install -r requirements-dev.txt
